import Spinner from '@/components/ui/Spinner';
import React from 'react';

const BlogsPageLoading = () => {
    return (
        <div>
            <Spinner/>
        </div>
    );
};

export default BlogsPageLoading;